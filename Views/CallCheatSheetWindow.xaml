<Window x:Class="Replixer.Views.CallCheatSheetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cheat Sheet" 
        SizeToContent="Height" Width="250"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False">

    <Border Background="#111214" 
            CornerRadius="12" 
            BorderThickness="1" 
            BorderBrush="#26272D"
            MouseLeftButtonDown="Header_MouseDown"
            Cursor="SizeAll">

        <StackPanel Margin="15">
            <TextBlock Text="ШПАРГАЛКА" 
                       FontSize="10" 
                       FontWeight="Bold" 
                       Foreground="WHITE" 
                       Margin="0,0,0,12" 
                       Opacity="0.8"/>

            <Separator Background="#333333"  Margin="0,0,0,12"/>

            <ItemsControl ItemsSource="{Binding Hints}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox VerticalAlignment="Center">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="CheckBox">
                                                    <Border x:Name="Box" 
                                                            Width="14" Height="14" 
                                                            CornerRadius="3" 
                                                            BorderThickness="1.5" 
                                                            BorderBrush="#35373C"
                                                            Background="Transparent">
                                                        <Path x:Name="CheckMark" 
                                                              Data="M2,7 L5,10 L12,3" 
                                                              Stroke="White" 
                                                              StrokeThickness="2" 
                                                              Visibility="Collapsed"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                            <Setter TargetName="Box" Property="BorderBrush" Value="White"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Box" Property="BorderBrush" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <TextBlock Grid.Column="1" 
                                       Text="{Binding}" 
                                       Foreground="#DBDEE1" 
                                       FontSize="13" 
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center"
                                       Margin="5,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>