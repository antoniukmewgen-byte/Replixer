<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="http://www.hardcodet.net/taskbar">

    <SolidColorBrush x:Key="TrayBg" Color="#111214" />
    <SolidColorBrush x:Key="TrayHover" Color="#35373C" />
    <SolidColorBrush x:Key="TrayText" Color="#DBDEE1" />
    <SolidColorBrush x:Key="TraySeparator" Color="#26272D" />

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TrayText}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border
                        x:Name="BgBorder"
                        Margin="5,2"
                        Background="Transparent"
                        CornerRadius="5">
                        <Grid Height="34">
                            <ContentPresenter
                                Margin="10,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                ContentSource="Header" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource TrayHover}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource TrayBg}" />
        <Setter Property="Width" Value="210" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border
                        Padding="0,5"
                        Background="{TemplateBinding Background}"
                        CornerRadius="9">
                        <StackPanel IsItemsHost="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource TraySeparator}" />
        <Setter Property="Margin" Value="10,3" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <tb:TaskbarIcon
        x:Key="NotifyIcon"
        DoubleClickCommand="{Binding OpenCommand}"
        IconSource="/Assets/Icons/IconApp.png"
        LeftClickCommand="{Binding OpenCommand}"
        ToolTipText="Replixer">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu Style="{StaticResource TrayContextMenuStyle}">

                <MenuItem Command="{Binding RecordCommand}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding RecordLabel}" VerticalAlignment="Center" />
                            <Ellipse
                                Width="7" Height="7"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#555860" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                                <Setter Property="Fill" Value="#F23F43" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>

                <MenuItem Command="{Binding OpenCommand}" Header="Відкрити Replixer" />

                <Separator Style="{StaticResource TraySeparatorStyle}" />

                <MenuItem Command="{Binding ExitCommand}" Header="Вийти" />

            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>

</ResourceDictionary>
