<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    xmlns:local="clr-namespace:EchoVault.ViewModels">

    <SolidColorBrush x:Key="TrayBg" Color="#111214"/>
    <SolidColorBrush x:Key="TrayHover" Color="#35373C"/>
    <SolidColorBrush x:Key="TrayText" Color="#DBDEE1"/>
    <SolidColorBrush x:Key="TraySeparator" Color="#26272D"/>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TrayText}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="BgBorder" 
                            Background="Transparent" 
                            CornerRadius="5" 
                            Margin="5,2">
                        <Grid Height="34">
                            <ContentPresenter ContentSource="Header" 
                                              VerticalAlignment="Center"
                                              Margin="10,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource TrayHover}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource TrayBg}"/>
        <Setter Property="Width" Value="210"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Grid.IsSharedSizeScope" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="9" 
                            Padding="0,5">
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <local:TrayViewModel x:Key="TrayVM"/>

    <tb:TaskbarIcon x:Key="NotifyIcon"
                    DataContext="{StaticResource TrayVM}"
                    IconSource="/Assets/Icons/IconApp.png"
                    ToolTipText="EchoVault">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu Style="{StaticResource TrayContextMenuStyle}">

                <MenuItem Header="EchoVault" IsEnabled="False" Opacity="1">
                    <MenuItem.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontWeight="Bold" Foreground="Gray" FontSize="11"/>
                        </DataTemplate>
                    </MenuItem.HeaderTemplate>
                </MenuItem>


                <MenuItem Header="Записати дзвінок" 
                          Command="{Binding RecordCommand}"
                          Foreground="{StaticResource TrayText}"/>

                <MenuItem Header="Відкрити EchoVault"
                          Command="{Binding OpenVaultCommand}"/>


                <MenuItem Header="Вийти"
                          Command="{Binding ExitCommand}"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>
</ResourceDictionary>