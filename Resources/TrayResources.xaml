<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <SolidColorBrush x:Key="TrayBg" Color="#111214"/>
    <SolidColorBrush x:Key="TrayHover" Color="#35373C"/>
    <SolidColorBrush x:Key="TrayText" Color="#DBDEE1"/>
    <SolidColorBrush x:Key="TraySeparator" Color="#26272D"/>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TrayText}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="BgBorder"
                            Background="Transparent"
                            CornerRadius="5"
                            Margin="5,2">
                        <Grid Height="34">
                            <ContentPresenter ContentSource="Header"
                                              VerticalAlignment="Center"
                                              Margin="10,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource TrayHover}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource TrayBg}"/>
        <Setter Property="Width" Value="210"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="9"
                            Padding="0,5">
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource TraySeparator}"/>
        <Setter Property="Margin" Value="10,3"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1"
                               Fill="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <tb:TaskbarIcon x:Key="NotifyIcon"
                    IconSource="/Assets/Icons/IconApp.png"
                    ToolTipText="Replixer"
                    LeftClickCommand="{Binding OpenCommand}"
                    DoubleClickCommand="{Binding OpenCommand}">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu Style="{StaticResource TrayContextMenuStyle}">

                <MenuItem IsEnabled="False">
                    <MenuItem.Header>
                        <TextBlock Text="Replixer"
                                   FontWeight="Bold"
                                   Foreground="Gray"
                                   FontSize="11"/>
                    </MenuItem.Header>
                </MenuItem>

                <Separator Style="{StaticResource TraySeparatorStyle}"/>

                <MenuItem Header="{Binding RecordLabel}"
                          Command="{Binding RecordCommand}"/>

                <MenuItem Header="Відкрити Replixer"
                          Command="{Binding OpenCommand}"/>

                <Separator Style="{StaticResource TraySeparatorStyle}"/>

                <MenuItem Header="Вийти"
                          Command="{Binding ExitCommand}"/>

            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>

</ResourceDictionary>
